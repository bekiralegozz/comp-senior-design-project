{
  "name": "smartrent-blockchain",
  "version": "1.0.0",
  "description": "SmartRent Smart Contracts",
  "scripts": {
    "compile": "hardhat compile --config hardhat.config.cjs",
    "deploy": "hardhat run scripts/deploy.js --network polygon --config hardhat.config.cjs"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "polygon",
    "smart-contracts",
    "nft"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}
