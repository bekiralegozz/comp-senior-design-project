import 'package:flutter/material.dart';

class CreateAssetScreen extends StatelessWidget {
  const CreateAssetScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Asset'),
      ),
      body: const Center(
        child: Text('Create Asset Screen - TODO'),
      ),
    );
  }
}
